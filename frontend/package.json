{
  "name": "crossfire-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.5"
  },
  "dependencies": {
    "an-array-of-english-words": "^2.0.0",
    "human-id": "^4.1.3"
  }
}
